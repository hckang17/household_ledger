# 📒 家計簿 (Household Ledger)

> **Flutter 製の個人家計簿アプリ** — 支出・収入・固定費の管理から、分析レポート・PDF 出力・データ移行まで。

韓国語版 → [README.md](README.md)

---

## 目次

1. [主な機能](#主な機能)
2. [画面構成](#画面構成)
3. [データ移行（エクスポート / インポート）](#データ移行エクスポート--インポート)
4. [PDF レポート出力](#pdf-レポート出力)
5. [技術スタック](#技術スタック)
6. [ビルド方法](#ビルド方法)

---

## 主な機能

### 💰 支出記録
- 日付・金額・カテゴリ・支払方法・メモを入力して支出を記録します。
- 月別カレンダービューとリストビューを併用できます。
- 消費区分（大分類 / 小分類）と支払方法タグを自由に設定できます。

### 🔁 固定費管理
- 家賃・サブスクリプション・保険料など毎月繰り返される固定費を別途管理します。
- 月別固定費合計と通常支出合計を分けて残高を計算します。

### 📈 収入管理
- 月収を登録し、予算に対する支出状況を把握します。

### 📊 分析レポート
- `fl_chart` ベースのビジュアライゼーションで支出パターンを一目で確認できます。
  - カテゴリ別 / 支払方法別の支出割合（円グラフ）
  - 日別支出推移（折れ線グラフ）
  - 最高・最低支出日の自動分析
- 特定の月または日付範囲を選択して絞り込み表示できます。

### 🗂 データ管理
- 検索条件（支払方法・期間・カテゴリ・内容・備考）でレコードをフィルタリングします。
- 検索結果を**一括選択 → 削除**または**タグ一括変更**できます。
- 2,000 件以上の大量データも遅延レンダリング（SliverList）でスムーズに表示します。

### 🌐 多言語 / 多通貨対応
| 設定 | サポート |
|------|---------|
| 言語 | 한국어 🇰🇷 / 日本語 🇯🇵 |
| 通貨 | ウォン (₩) / 円 (¥) |

---

## 画面構成

| 画面 | 説明 |
|------|------|
| ホーム | 今月の収入・支出・残高サマリー、最近の支出リスト |
| 支出記録 | カレンダー + リスト形式で支出を確認・入力 |
| 固定費 | 月別固定費の管理 |
| 収入 | 月収の入力・確認 |
| 分析 | グラフによる支出分析 |
| データ管理 | 検索・一括削除・タグ変更 |
| エクスポート | CSV 暗号化バックアップファイルの作成 |
| インポート | バックアップファイルの復元 |
| レポート生成 | PDF レポートの生成・共有 |
| 設定 | 言語・通貨・予算・タグ管理 |

---

## データ移行（エクスポート / インポート）

機種変更やデータのバックアップを行いたいときに使用します。

### エクスポート手順

1. **データ管理**メニューから**エクスポート**を選択します。
2. エクスポートする**期間**を選択します（全期間 / 特定月 / 日付範囲）。
3. **認証キー**を入力します（復元時に同じキーが必要です）。
4. **エクスポート**ボタンを押すと、暗号化された `.csv` ファイルが生成されます。
5. ファイルをメール・クラウド・USB など好きな方法で保存します。

> ファイルはメールアドレスと認証キーの組み合わせで署名されます。  
> 署名が一致しない場合は復元がブロックされますので、**キーは必ずメモしておいてください。**

### インポート手順

1. **データ管理**メニューから**インポート**を選択します。
2. 保存しておいた `.csv` ファイルを選択します。
3. エクスポート時に使用した**認証キー**を入力します。
4. **インポート**ボタンを押すと、既存データとのマージなしに全データが復元されます。

> ⚠️ インポートは現在のデータを**上書き**します。事前に現在のデータをエクスポートしてバックアップしておいてください。

---

## PDF レポート出力

月次締め・確定申告・家族への共有など、さまざまな用途で PDF レポートを生成できます。

### 含めることができるセクション

| セクション | 内容 |
|-----------|------|
| 概要サマリー | 総収入 / 総支出 / 残高 / 固定費合計 |
| カテゴリ別支出 | カテゴリ・小分類別の金額と割合 |
| 支払方法別支出 | 決済手段別の金額と割合 |
| 支出 Top 10 | 最大の支出項目 10 件 |
| 固定費詳細 | 固定費全リスト |
| 支出明細（付録） | 対象期間の全取引履歴 |
| グラフ | カテゴリ円グラフ、日別支出折れ線グラフ |

### 出力手順

1. **データ管理**メニューから**レポート生成**を選択します。
2. レポートに含める**セクション**にチェックを入れます。
3. 集計する**期間**を設定します。
4. 必要に応じて**パスワード**を設定します（PDF 閲覧時に入力を要求）。
5. **生成**ボタンを押すと PDF が作成され、**共有**ボタンで他のアプリに渡せます。
6. 生成したレポートは**レポート一覧**に保存され、いつでも再閲覧・再共有できます。

---

## 技術スタック

| 役割 | パッケージ |
|------|-----------|
| UI フレームワーク | Flutter 3.x |
| 状態管理 | `flutter_riverpod ^3.0.3` |
| ローカル DB | `sqflite ^2.4.2` |
| 設定保存 | `shared_preferences ^2.5.3` |
| グラフ | `fl_chart ^0.69.0` |
| PDF 生成 | `pdf ^3.13.0` |
| PDF 印刷・共有 | `printing ^5.15.0` |
| ファイル選択 | `file_selector ^1.0.3` |
| ファイルを開く | `open_file ^4.0.0` |
| ファイル共有 | `share_plus ^10.1.4` |
| 暗号化・署名 | `crypto ^3.0.3` |
| 国際化 | `intl ^0.20.2` |
| チュートリアル | `showcaseview ^3.0.0` |

---

## ビルド方法

```bash
# 依存関係のインストール
flutter pub get

# 実行（開発）
flutter run

# APK ビルド（Android）
flutter build apk --release

# App Bundle ビルド（Google Play）
flutter build appbundle --release

# Windows ビルド
flutter build windows --release
```

> **最低 Flutter バージョン**: 3.x  
> **対応プラットフォーム**: Android, Windows（iOS・macOS はビルド環境次第で対応可能）

---

*個人プロジェクトです。お問い合わせ・フィードバックは Issues タブをご利用ください。*
